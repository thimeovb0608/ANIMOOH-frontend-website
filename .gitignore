# Temporary local preview snapshots (not part of the real server-rendered blog)
_preview-*.html
try-node.js

# Node dependencies (installed by Vercel, not committed)
node_modules/

# Confidential Adsolut API documentation — local reference only, never pushed
adsolut-docs/
